{
  "name": "@paddlelink/desktop",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "cd ../.. && npm run build -w @paddlelink/shared --if-present",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@paddlelink/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
